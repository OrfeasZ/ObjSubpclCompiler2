#pragma once

#include <Misc/StdAfx.h>

namespace Util
{
	class Utils
	{
	public:
		static std::string Join(const std::vector<std::string>& p_Strings, const std::string& p_Separator);
	};
}